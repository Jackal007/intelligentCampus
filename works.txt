1,把学生画像的再改进一点,(要不要增加字段?)



风险识别：
1.中文乱码


接下来要做的
1.数据清理，有的学生有，有的学生没有xxxxx
2.myconfig代码
3.提高统计速度（可以考虑数据库分区）
4.把代码写成扩缩性强的代码，易于修改

    db = MyDataBase.MyDataBase()
    conn = db.getConn()
    executer = db.getExcuter()
    
    # 从这边读取到所有学生的id
    sql = "select student_id from score"
    executer.execute(sql)
    students = []
    for i in executer.fetchall():
        students.append(i[0])